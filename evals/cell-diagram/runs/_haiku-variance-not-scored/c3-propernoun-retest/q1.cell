title SearchCell

component api as "Search API" api
component indexer as "Indexer" worker
component indexStore as "Index Store" database

north -> api
api -> indexStore
indexer -> indexStore
indexer -> east
